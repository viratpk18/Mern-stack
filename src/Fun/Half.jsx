import React from 'react';
import Stom from './Stom';

const Half = ({name}) => {
    return (
        <div>
            <h2>Halfboil</h2>
            <Stom name={name}/>
        </div>
    );
};

export default Half;